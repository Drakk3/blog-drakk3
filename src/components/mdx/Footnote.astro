---
interface Props {
  id: string;
}
const { id } = Astro.props;
---
<sup><a href={`#fn-${id}`} id={`fnref-${id}`} class="text-xs font-medium text-zinc-500 hover:text-zinc-800 dark:text-zinc-400 dark:hover:text-zinc-200">[{id}]</a></sup>
